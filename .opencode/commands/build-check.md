---
description: Run production build and fix any errors
agent: build
---

Run `pnpm build` to verify the production build succeeds. If there are errors, fix them. Then run `pnpm build` again to confirm. Remember this is a static site for GitHub Pages -- no SSR features allowed.
