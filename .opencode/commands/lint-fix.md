---
description: Run lint and auto-fix errors
agent: build
---

Run `pnpm lint` to check for errors. If there are lint errors, run `pnpm format` to auto-fix them. Then run `pnpm lint` again to verify all issues are resolved. If errors persist, fix them manually.
